import { Quote } from "../../../types/types"

export default function QuoteEdit({ quote }: { quote: Quote }

) {
    return (
        <div>
            <p>You are currently at Quote-Edit!</p>

        </div>
    )
}