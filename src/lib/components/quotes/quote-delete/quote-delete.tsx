import { Quote } from "../../../types/types"

export default function QuoteDelete({ quote }: { quote: Quote }

) {
    return (
        <div>
            <p>You are currently at Quote-Delete!</p>

        </div>
    )
}