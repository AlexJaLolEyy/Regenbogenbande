import { Picture } from "../../../types/types"

export default function PictureDelete({ picture }: { picture: Picture }

) {
    return (
        <div>
            <p>You are currently at Picture-Delete!</p>

        </div>
    )
}