import { Picture } from "../../../types/types"

export default function PictureEdit({ picture }: { picture: Picture }

) {
    return (
        <div>
            <p>You are currently at Picture-Edit!</p>
        </div>
    )
}